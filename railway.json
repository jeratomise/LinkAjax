{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm run build -w ajax-web"
  },
  "deploy": {
    "startCommand": "npm run start -w ajax-web",
    "healthcheckPath": "/login",
    "restartPolicyType": "ON_FAILURE"
  }
}
